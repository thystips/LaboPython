while true:
   hauteur_max=0
   numéro_max=0
   numéro=0
   #x=0

while x<8:     #tant que x<8
#   x = x+1     x=x+1
#    (x+=1)
#pour numéro allant de 0 à 7
       #lit la hauteur de la montagne numéro
    mountain_h = int(input))
    #si cette montagne est la plus haute haute que les précédente
    if mountain_h > hauteur_max
        #hauteur-max <- hauteur de cette montagne
        hauteur_max=mountain_h
        #numéro-max <- de cette montagne
        numéro_max=numéro
    numéro += 1
    #afficher le numéro de la montagne la plus haute
    print(numéro_max)